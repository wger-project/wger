# Changelog for the next release

- Body weight entries are now stored as measurements in a weight category
  that every user gets automatically, existing entries are
  migrated. Official categories cannot be deleted.
